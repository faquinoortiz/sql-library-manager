extends layout

block content
  a(href='/books/new', class='button') Create New Book
  h1= title
  table
    thead
      tr
        th Title
        th Author
        th Genre
        th Year
    tbody
      each book in books
        tr
          td= book.title
          td= book.author
          td= book.genre
          td= book.year